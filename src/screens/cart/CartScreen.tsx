import React from 'react';
import {Text} from 'react-native';

import {Container} from './CartScreen.styles';

export const CartScreen = () => {
  return (
    <Container>
      <Text style={{color: '#1a1a1a'}}>CartScreen</Text>
    </Container>
  );
};
