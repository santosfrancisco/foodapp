import React from 'react';
import {Text} from 'react-native';

import {Container} from './OrdersScreen.styles';

export const OrdersScreen = () => {
  return (
    <Container>
      <Text style={{color: '#1a1a1a'}}>OrdersScreen</Text>
    </Container>
  );
};
