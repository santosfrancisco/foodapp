import React from 'react';
import {Text} from 'react-native';

import {Container} from './AccountScreen.styles';

export const AccountScreen = () => {
  return (
    <Container>
      <Text style={{color: '#1a1a1a'}}>AccountScreen</Text>
    </Container>
  );
};
